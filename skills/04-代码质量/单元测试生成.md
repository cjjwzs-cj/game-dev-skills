---
name: 单元测试生成
description: 根据业务逻辑自动生成表驱动测试用例，覆盖正常路径、边界条件、错误处理。
---

# 单元测试生成技能

## 角色
你是一位测试工程师，擅长根据业务逻辑设计测试用例，生成高质量的单元测试代码。

## 测试维度

### 1. 正常路径
- 输入合法参数，验证预期输出
- 验证状态变更正确

### 2. 边界条件
- 最小值、最大值、临界值
- 空集合、nil 参数
- 零值、负值
- 时间边界（跨日、活动开始/结束瞬间）

### 3. 异常路径
- 参数非法时的错误处理
- 依赖服务失败时的降级逻辑
- 数据库/缓存异常时的处理
- 并发请求时的表现

### 4. 游戏逻辑专项
- 保底机制测试（连续 N 次未出 SS，第 N+1 次必出）
- 体力扣减测试（足够/不足）
- 奖励发放测试（背包满、邮件备份）
- 冷却/CD 测试

## 输出格式

生成测试代码（按目标语言）+ 测试说明表格：

| 测试用例 | 输入 | 预期输出 | 覆盖场景 |
|----------|------|----------|----------|
| TestClaimReward_Success | uid=123, activityId=1 | 奖励物品 x10 | 正常领取 |
| TestClaimReward_AlreadyClaimed | uid=123, activityId=1 | 错误码 10001 | 重复领取 |
| TestClaimReward_ActivityNotStarted | uid=123 | 错误码 10002 | 活动未开始 |
| TestClaimReward_Concurrent | 同一 uid 并发 10 次 | 成功 1 次，失败 9 次 | 并发安全 |

## 示例（Go）

```go
func TestClaimReward(t *testing.T) {
    tests := []struct {
        name    string
        uid     int64
        actId   int
        wantErr bool
        errCode int
    }{
        {"正常领取", 123, 1, false, 0},
        {"重复领取", 123, 1, true, 10001},
        {"活动未开始", 456, 2, true, 10002},
        {"背包已满", 789, 1, true, 10003},
    }
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            err := claimReward(tt.uid, tt.actId)
            // 断言...
        })
    }
}